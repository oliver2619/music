import { SelectUnitDirective } from './select-unit.directive';

describe('SelectUnitDirective', () => {
  it('should create an instance', () => {
    const directive = new SelectUnitDirective();
    expect(directive).toBeTruthy();
  });
});
