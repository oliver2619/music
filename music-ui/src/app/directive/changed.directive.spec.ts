import { ChangedDirective } from './changed.directive';

describe('ChangedDirective', () => {
  it('should create an instance', () => {
    const directive = new ChangedDirective();
    expect(directive).toBeTruthy();
  });
});
