import { NumberChangedDirective } from './number-changed.directive';

describe('NumberChangedDirective', () => {
  it('should create an instance', () => {
    const directive = new NumberChangedDirective();
    expect(directive).toBeTruthy();
  });
});
