import { ChangingDirective } from './changing.directive';

describe('ChangingDirective', () => {
  it('should create an instance', () => {
    const directive = new ChangingDirective();
    expect(directive).toBeTruthy();
  });
});
