import { SelectSnapDirective } from './select-snap.directive';

describe('SelectSnapDirective', () => {
  it('should create an instance', () => {
    const directive = new SelectSnapDirective();
    expect(directive).toBeTruthy();
  });
});
