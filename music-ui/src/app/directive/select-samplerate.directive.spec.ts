import { SelectSamplerateDirective } from './select-samplerate.directive';

describe('SelectSamplerateDirective', () => {
  it('should create an instance', () => {
    const directive = new SelectSamplerateDirective();
    expect(directive).toBeTruthy();
  });
});
